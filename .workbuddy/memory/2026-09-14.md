# 2026-09-14 工作日志

## 文档全面同步与对齐（2026-09-14 凌晨，承接 09-13 的 Stage 4/5 开发）

**背景**：Stage 4 CBR、Stage 5.1 类目树、Stage 5.2 主动学习代码已落地并提交，
但规划文档（`docs/adaptive-semantics/03-implementation-plan.md`）与实际实现存在 **5 处偏差**，
且顶层文档（ARCHITECTURE / 开发计划 / README / MEMORY / DAILY_LOG）均未反映自适应语义机制。
本次执行全面同步，逐条修正并输出对齐清单。

### 修正的 5 处规划偏差（核心）

| # | 规划原写 | 实际实现 | 依据 |
|---|---|---|---|
| 1 | `migration_002_build_tree.py` | **`migration_003_build_tree.py`** | `002` 已被 preset 别名占用 |
| 2 | 人审用 WebSocket / SSE 推送 | **轮询** `GET /api/adaptive/pending-feedbacks` | FastAPI 无 WS/SSE 基建 |
| 3 | 反馈写 `episodes` 数据库表 | **写 JSONL 文件**（`webui/data/pending_feedbacks.jsonl`） | `episodes` 表从未建立 |
| 4 | 新建 `api/adaptive_semantics.py`，端点 `/api/categories/{id}/feedback` | **沿用 `adaptive.py`**，端点 `/api/adaptive/categories/{id}/feedback` | 与 Stage 1~4 一致 |
| 5 | 前端直接写 `components/CategoryFeedbackModal.tsx` | **需新建 `components/` 目录**（当前不存在），并抽 `AutoTuneSuggestion.tsx` | 用户决策 |

另修正测试数：CBR 3→**7** 例、类目树 4→**7** 例、主动学习 3→**7** 例。

### 更新的文档（7 份）

1. **`docs/adaptive-semantics/03-implementation-plan.md`** — 修正 Stage 3/4/5 全部偏差
   - Stage 3 标注「核心骨架完成（🟡）」+ 未做项说明
   - Stage 4 标注「✅ 已完成（commit b9eb5b0）」
   - Stage 5.1/5.2 标注「✅ 已完成」+ 5 处偏差修正注记
   - Stage 5.3 标注「⏳ 待开始」+ 目录新建说明
   - 6.2 文件清单修正（不新建 adaptive_semantics.py）
   - 测试矩阵修正（7/7/7 + 全量基线 128）
2. **`docs/ARCHITECTURE.md`** — 新增 **§8 自适应语义匹配机制**
   - 8.1 分层位置图 / 8.2 Stage 完成矩阵 / 8.3 三条架构决策 / 8.4 测试环境
3. **`docs/UNIVERSAL_LAYER_ENGINE_DEVELOPMENT_PLAN.md`** — 更新「最后更新」为 2026-09-14
   - 新增自适应语义 Stage 1~5.2 状态注记 + 4 条关键架构决策 + 测试基线 128
4. **`docs/MEMORY.md`** — 新增 **ADR-021 ~ ADR-025**（自适应语义 5 条决策）
5. **`docs/DAILY_LOG.md`** — 新增「最新状态指针（2026-09-14 凌晨）」覆盖旧指针
6. **`README.md`** — 新增「一之二、自适应语义匹配机制」章节 + 文档表新增 adaptive-semantics/
7. **`.workbuddy/memory/2026-09-13.md`** — 补充 Stage 4/5.1/5.2 完整实施记录

### 提交记录（本轮同步前的代码提交，均已完成）

- `21e8f90` Stage 3 反馈闭环 + 影子进化核心骨架
- `b9eb5b0` Stage 4 案例推理库 CBR
- `bfd4e2e` docs: Stage 3/4 记录 + 测试环境事实入库
- `b74ab10` Stage 5.1 类目树管理
- `ae39733` Stage 5.2 主动学习

### 剩余工作

- **Stage 5.3**：前端人审弹窗（`CategoryFeedbackModal.tsx`）+ 新建 `components/` 目录
  + 抽离 Stage 2 Auto-Tune 卡片（`AutoTuneSuggestion.tsx`）+ 3 秒轮询集成
- 文档同步本身的 git 提交（本次会话结束前完成）

## Stage 5.3 前端人审弹窗完成（2026-09-14 00:20，commit b7241a3）

### 交付
- **新建目录** `webui/frontend/src/components/`（此前不存在）
  - `AutoTuneSuggestion.tsx`：Stage 2 卡片从 App.tsx 抽出（App.tsx -97 行），导出类型 `AutoTuneSuggestionData`
  - `CategoryFeedbackModal.tsx`：人审弹窗（confidence 进度条 + accept/rename/delete/merge）
- **App.tsx**：改用 `<AutoTuneCard />`；新增 3 秒轮询 `GET /api/adaptive/pending-feedbacks?limit=1`
  - 弹窗打开时暂停轮询；失败静默；逐类目裁决，全部处理完才关闭

### 实现取舍（勿回退）
- **merge 用文本输入目标 id**，不用下拉：后端 merge 是 TODO，且 `GET /api/adaptive/categories`
  只返回 `display_name`（无 id），不为一个未实现功能扩展 API
- 轮询而非 WS/SSE（延续 ADR-022）

### 验证
- `npx tsc --noEmit --skipLibCheck` **零错误**
- 真实 HTTP 冒烟（FastAPI TestClient）：
  GET pending 200（1 条）→ POST accept 200（权重 ×1.1 生效）→ 复查剩余 0
- **脏数据已清理**：删测试待审队列；回滚 water_ripples 权重 1.1→1.0、n_accept 1→0
  （教训：冒烟会真实改动 category_prompts，测后必须回滚）
- 引擎 128 passed / 2 skipped；WebUI 28 OK

### Stage 5 全部完成（5.1/5.2/5.3）
剩余仅 `test_adaptive_stage5_e2e.py` 未建（功能已由单元测试 + HTTP 冒烟覆盖）。

## rename/delete/merge 真实实现 + Stage 5 e2e（2026-09-14 00:45，commit 906256b）

### 三个操作从 TODO 转真实实现
- **rename**：更新 `categories.name_zh / name_en / name_template`
  - name_template 保留原序号前缀（如 `NN_`）后按 `{name_zh}_{name_en}` 重建
- **delete**：**软删除**（置 `deleted_at`）
  - 选软删而非硬删的关键理由：categories 被 category_prompts / category_material_affinity /
    category_priors 外键 **CASCADE** 引用，硬删会连带清空学到的权重且不可恢复；
    历史 episode JSONL 也记录 category_id，硬删会让回放松散引用
  - 软删后由 `db_manager` 主查询 `c.deleted_at IS NULL` 过滤 → 等价于不可见
- **merge**：源 prompts 迁移到目标（同名 prompt 取权重较大者，绕 `UNIQUE(category_id, prompt)`）
  + 源类目软删；拒绝「缺 target / 目标不存在 / 目标==源」
- 统一返回 `{ok, action, detail}`；失败不再静默，API 明确 400

### 配套
- `migrations/migration_004_feedback_ops.py`（新）：categories 加 `deleted_at`（幂等），已应用到真实库
- `schema.sql` 同步 DDL + `idx_cat_deleted` 索引（保证新建库结构一致）
- `api/adaptive.py` 反馈端点改读 `ADAPTIVE_DB_PATH`（与 /stats 一致，可测试隔离）

### e2e 测试（tests/test_adaptive_stage5_e2e.py，14 例全绿）
树祖先链 / 建树数 / 识别入队 / accept 权重 / rename / delete 软删+不可见 /
merge 迁移+拒绝三种非法 / HTTP 轮询+提交+复查+非法 400

### 隔离原则（重要，已固化为测试范本）
- DB：复制真实库到临时副本 + `ADAPTIVE_DB_PATH` 指向副本
- 待审队列：备份真实 `pending_feedbacks.jsonl`，tearDown 还原
- 测试后核对真实库零污染（name 未变 / 权重 1.0,0.85 原值 / 无软删 / 无残留队列）
- 教训来源：Stage 5.3 冒烟时直接打真实库，把 water_ripples 权重改了 ×1.1 才回滚

### 验证
引擎 142 passed / 2 skipped（128 + 新增 14）；WebUI 28 OK
