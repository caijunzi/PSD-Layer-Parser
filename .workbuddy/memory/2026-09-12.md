# 2026-09-12 工作日志

## 三类攻坚 P1/P2/P3（09:50-10:10，提交 0920feb + dd262c5）

**背景**：接手「04D 远山 / 05A 寒林 / 04B 平渚」三类未成层攻坚。

**关键突破（新增第 4 条产层通道：区域先验 SAM）**：
- 根因：DINO 对弱语义类给出粗框 → SAM 输出全画布弥散掩模（bbox 74-75%）
- 解法：preset 给类配 region（归一化框）+ region_boxes → 区域内多框采样，
  multimask 取**面积最小合理候选**（局部对象优先）→ 绕过 DINO 定位职责
- 代码： + segment_objects 2.5 段
- source 标记 ，产出仍过质量门/弥散门/披露

**三类结果**：
| 类 | 结论 | 数据 |
|---|---|---|
| 05A 寒林 | ✅ region SAM | region=[0.36,0.25,0.76,0.80] bbox 75.1%→21.6%，1558x1079 贴合中央5-6棵枯树 |
| 04B 平渚 | ✅ region SAM | region=[0.26,0.56,0.80,0.90] 2176x628 bb17.5%，中央树下坡岸带 |
| 04D 远山 | ❌ 不可自动 | 三种尝试失败（密度带错误层 / region 输外框 / region 弥散74.5%+IoU0.012）；撤销配置+note 记录 |

**失败方案存档（避免重走）**：
- Frangi 线状簇：与树位置先验重叠仅 12-14%（响应多为金地颗粒纹理）
- SAM 密集小框 44 框（160px 网格）：合并 bbox 68.5% 仍弥散
- 密度精修平滑/原始 D：细线被稀释

**层数 12 → 14**；configured 12 类中 11 类产出，rejected 仅 04D。

**画面认知修正（重要）**：金地屏风实际构图 = 左大片空金地+雁群 / 中央一丛枯树群 /
中央下部横向坡岸 / 左下水中石矶 / 右侧山峦叠嶂（浓墨）+ 山间草堂+高士 /
**右上另有一大丛枯树（未被 05A region 覆盖，优化项）** + 淡墨远山 + 题跋印章。

**未决**：右上枯树丛可扩展 05A region 或加 05A_02；04D 需人工/更优源图。
测试：引擎 66 项全绿。


## 底板修复 + 对象级穷尽拆分 + 硬件对照实测（10:16-11:00，提交 3ad3f55 / 45549a2）

**① 底板层缺陷修复（用户对照 V2 指出）**：旧实现底板去墨只用语义层掩模并集 →
未被语义类识别的内容（山峦等）残留底板。改为全画幅墨迹掩模（gray<median-12，
折痕保留）+ 新增  承载未分类内容（合成零丢失）。
实测底板纯净度（dark_px%）：雁群区 0.7 / 山峦区 1.1 / 枯树区 0.4 / 纯金地 0.0
（V2 对照：0.1 / 0.5 / - / 1.6）；**唯一差距**：中央石矶区 9.7%（V2 0.9%）待优化。

**② 对象级穷尽拆分（用户核心诉求"每只/每棵/每块各自一层"）**：11 层 → **43 层**
- 新机制1 blob_instances（墨点连通域实例化，纯形态学）：雁 21 只逐只
  （DINO 对 30-60px 雁漏检实测：th=0.22 仅 1 有效框，其余石矶假阳性）
- 新机制2 多区域 region SAM（regions）：枯树两丛 → 05A_01/_02
- 04C 石矶 region+instance_split → 8 块逐块
- 长宽比过滤（>4）排除折痕细长条假阳性
- 遗留假阳性：题跋墨字被当雁（待 region 收紧）

**③ 硬件/本地模型对照实测（回答"是否花架子"）**：
- imagecodecs SIMD: 225 MB/s vs 纯 Python 2.0 MB/s = **114x** ✅ 决定性
- LaMa GPU vs CPU：1000x488 4.59s vs **2.05s**（GPU 慢 2.2x）；
  2000x976 预热后 4.50s vs **3.24s**（GPU 慢 1.4x）→ **GPU 对 LaMa 无收益**
- 本地模型（DINO+SAM2）贡献 43 层对象级分层的根本质量 ✅
- 分割默认 CPU = 有意设计（GPU 破坏 RK-16 逐像素复现 + 实测无净收益）
- RK-16/PLATE 合规：plate 线不用生成模型（telea_ns 确定性）
测试：引擎 68 项全绿。

## 深度审计（11:00-12:40，提交 8cc0ea2）—— 用户质疑"还有没有同类坑"

**审计 8 维度**：层属性/分辨率真实性/层间重叠/内容丢失定量/合成等价性/底板纯净度/plate 线/manifest 一致性

**🔴 已修复 3 个隐性缺陷（均属"层存在但核心价值受损"类）**：
1. 底板去墨掩模范围错 → 双保险（灰阶 median-20 ∪ 语义层并集）
2. panel 参考扇自带墨迹被复制 + 未写回 clean_bg（雁/岩石残留主因）→ 清理并写回
3. 墨迹核心 alpha 不足（0.6）→ max(模糊, 二值×0.95)
（另验证 LaMa 逐像素补全路径更差：山峦残留 1.1%→21.8%，已回滚记录）
实测：雁群 0.67%→0.00% / 石矶 9.70%→0.01% / 山峦 1.10%→0.02% / 枯树 0.40%→0.00%

**🟢 已验证正常（消除"垃圾"担忧）**：
- **内容零丢失**：全墨迹 55.07M px 中，仅 21px 既未承载于层也未保留于底板（0.00004%）
- 层像素非空（alpha 20-71%）、分辨率真实（shape==bbox）、同类实例无重复（IoU<0.6）
- plate 线正常：CMYK ✓ 底板 composite RGB(184/157/112) 金地色 ✓ 纯净性 ✓ manifest TAC 252-300% 合规
- 测试 68 项全绿

**🟡 待修（非致命）**：manifest 缺底板条目（42 vs 43）；层顺序混乱（实例乱序）；
04C_01/02 bbox 相同待视觉确认；雁实例假阳性（题跋墨字）

**🟠 审计盲区（诚实）**：真实 Photoshop 打开验证（仅 psd_tools）；前端 UI 端到端（React onClick 自动化障碍）

**审计脚本**：scratch/deep_audit_layers.py（层属性/重叠）、audit_synth3.py（合成等价性）、
audit_lost.py（内容丢失定量）、audit_plate.py（plate 线）

## 审计待修 4 项全部修复（12:00-12:10，提交 4571421）

1. **实例去重**（IoU>0.9）：DINO 低阈值重叠框 → SAM 产出几乎相同的实例层
   （04C_01/_02 实测 IoU=1.000）→ 两层路径均加去重，04C 8→7 层
2. **双通道冲突**：配 blob_instances 的类跳过 SAM 实例拆分（同名键隐式覆盖 +
   白跑 DINO/SAM）→ 端到端 9m52s→5m34s
3. **层序规范化**：按名称前缀排序 → PSB 呈现 底板02→内容03-09→装饰10A/10B→残层11
   （修复前 39/43 乱序）
4. **manifest 支撑层补录**：底板条目补录后 manifest 层数 == PSB 层数（42==42）
5. **雁假阳性**：右上雁群带右边界 0.99→0.85 避开题跋区（题跋墨字曾被误判为雁）

验证脚本：scratch/verify_fix4.py（层序/去重/manifest/假阳性 4 项自动核查）
后续建议：把审计脚本固化为交付前必跑门（tools/audit_*.py）；层序验证脚本的
判定方向需修正（PSB 列表 = 从顶到底，应为名称降序）。

## 8 维审计门正式接入交付流水线（12:19-12:45，提交 d47c4c6）

**tools/audit_psb.py**（CLI + 可导入）：① 层属性 ② 分辨率真实性 ③ 实例重复
④ 内容承载（含残层口径——排除残层会虚报 1.03%→18.38%）⑤ 合成等价性（多尺度）
⑥ 底板纯净度（通用判定：存在+非空；分区域 dark 品类相关→披露不判失败）
⑦ plate 合规 ⑧ manifest 一致。阈值经 V2 同口径校准（base_dark 8%）。
CLI exit 2 = 拦截。

**流水线接入**：task_manager 任务完成后 asyncio 异步跑审计 → result.audit.json
落盘 + WS audit 消息（dims/issues/download_url）；审计异常不影响任务状态。
下载接口 /api/download/{id}/audit；前端完成态「交付审计·8 维门禁」卡片。

**端到端实测**：damask plate（122614）完成后 125s 自动生成 audit.json 8/8 通过
（⑥ 正确识别 Fabric_Base；⑦ TAC 252.16 ✓ 纯净性 ✓）；山水 design 8/8。
两品类产物在新审计门下全部通过。

**审计期间连带修复**：④ 口径（残层是内容载体）；⑥ 通用化（底板名写死
Gold_Base_Clean 对壁布误报）；① layer_count 计数含全部层。
测试：68/28/tsc 全绿。

## WEB 全流程模拟实测报告（山水 japanese_screen_gold / both 模式，13:25-13:41）
模拟人类点击 WEB（GET /api/presets → POST /api/upload → POST /api/process → WS 进度）
跑通山水示例，脚本 scratch/sim_webui_flow.py 全程采样。

**关键数据**：
- 端到端 897.8s（≈15min）；引擎 699s（11.65min）；审计 ~196s；交互步骤仅 1.4s。
- 8 维审计门 **8/8 PASS，0 issue**；42 层；generated_pixel_ratio=0.0（零生成内容）；TAC 300% 达标。
- 产物 design.psb 1.51GB + plate.psb 1.79GB。
- 硬件：**纯 CPU（无 GPU）**，引擎峰值 RSS 21.5GB（整机 98.9% 内存临界），引擎 CPU 峰值 1096.5%（≈11/16 核）；
  磁盘读写合计 ~12.8GB（SSD 必需）。
- 本地模型参与：GroundingDINO(661MB,30-357s) / SAM2(149MB,贯穿435s) / RealESRGAN(64MB,104次,547s) /
  LaMa(198MB)+telea_ns(572s) / plate_separation(CPU) / PSDCompiler。

**结论**：机制层图无关已验证（壁布+山水均过审计）；代价是 CPU 慢+内存临界。
报告：scratch/sim_report.md + scratch/sim_report.json。后端仍由工具后台托管运行(N0xD9p @8099)。

## 重要修正：本机有 GPU+NPU，之前"纯CPU"结论是误判
用户指出机器有 GPU 和 NPU。核查：torch 2.7.1+cu128 报告 cuda_available=True，设备=NVIDIA GeForce RTX 5070 Laptop GPU；
OpenVINO available_devices=['CPU','GPU.0','GPU.1','NPU']（GPU.0=Intel Arc 140T iGPU，GPU.1=RTX 5070 dGPU，NPU=Intel NPU）。
之前误判根因：只信了坏掉的 nvidia-smi 管道（Git Bash 下 head 不存在导致整条失败），没用 torch.cuda.is_available() 核实。

引擎真实设备策略（engine/providers/）：
- 分割 SAM2/DINO：**写死 CPU**（grounded_sam_provider 注释：GPU 实测更慢30%且破坏逐像素复现；设 ULS_SEGMENT_DEVICE=cuda 才启用）。
- RealESRGAN 超分：**故意避开 RTX 5070**，realesrgan_provider:123 注释 Blackwell sm_120 OpenCL 编译深度图会挂起 → 自动切 Intel Arc 140T(GPU.0)。
- LaMa 补全：按 robust_performance profile primary_device=GPU.1 → RTX 5070。
=> 那次 15min 运行并非纯CPU（Arc 跑超分、5070 跑补全），但最重的分割在 CPU，且 RTX 5070 被超分绕开仅用于补全。

下一步待用户决策：A 重跑并正确采集 GPU 遥测(nvidia-smi+抓设备选择日志)出修正报告；B 设 ULS_SEGMENT_DEVICE=cuda 实测 Blackwell 分割快慢；
C 排查 Blackwell OpenCL 挂起让超分真用 5070。后端仍 N0xD9p @8099 运行中。

## 修复：RealESRGAN 超分绕开 RTX 5070 的过时绕行（已验证）
realesrgan_provider._init_backend 原硬编码：preferred==GPU.1 时无条件切 GPU.0(Arc)，注释称
"Blackwell sm_120 OpenCL 编译深度图挂起"。实测探针(scratch/probe_gpu1_esrgan.py)证明：GPU.1 上
compile_model 仅 3.4s、单切片推理 17.3s，**不挂起**——该绕行是过时/未实测的，致 5070 闲置。
改为候选顺序尝试 [GPU.1 → GPU.0(shield) → CPU]，首个编译成功者胜出，失败自动回退（保留三模型源尝试）。
单元验证：RealESRGANProvider(robust_performance) → backend=openvino_GPU.1_cached_ir (8s)。修复未提交。
注意：最重阶段(分割 SAM2/DINO)仍写死 CPU（grounded_sam_provider），仅开超分上 5070 不会让整跑大幅提速，
要显著提速需另做选项B(ULS_SEGMENT_DEVICE=cuda 实测 Blackwell 分割快慢)。

## 重大修正（9-13）：超分上 5070 是性能灾难，已回退 Arc
用户在 9-13 重跑发现"比之前更慢"。定位：plate.psb 准时(5.2min)，但 design.psb >24min 未出（上次 11.4min）。
engine 100% 占 5070、显存 7.5/8.15GB。根因=我上一轮把 RealESRGAN 从 Arc(GPU.0)强制挪到 5070(GPU.1)。

A/B 基准（scratch/bench_esrgan_gpu.py，512x512 稳态切片）：
- Intel Arc (GPU.0)：compile 4.6s，**1.299 s/片**
- RTX 5070 (GPU.1)：compile 2.9s，**92.71 s/片**
- **Arc 比 5070 快 71.4x**（5070 走 OpenVINO 通用后端无原生 CUDA EP；Arc 有原生 Level Zero）
故 design 线从 5.7min → >24min。原代码"避开 5070"在性能上完全正确，只是注释把原因错写为"挂起"。

处置：停掉慢跑(kill driver+engine，GPU 已释放)，realesrgan_provider 改为
robust_performance(护盾=Arc) 时优先护盾设备 GPU.0；5070 直通档(shield=CPU)尊重首选不被改道。
单元验证：robust_performance→GPU.0，5070档→GPU.1。修复未提交。
教训：探针只验"能跑/不挂起"不够，必须测速；"改用独显"不等于"更快"。

## 深挖：+43% 慢的根因=LaMa 也跑在 5070 上（已一并回退 Arc）
带 GPU 遥测重跑(9-13 10:38, task_20260913_103835_3cfc3b)：engine 999.1s vs 基线 699s(+43%)，审计全过(42层)。
device_logs 确认：超分已回 Arc(openvino_GPU.0_cached_ir)✅；但 LaMa Active Primary Device:[GPU.1]。
时间线：461s 之前与基线几乎一致；差异全在 461s 后(基线231s vs 本次531s)；其中 t=465→826s 有 360s 静默黑洞(仅心跳)
=LaMa 大图补全跑在 5070。GPU遥测：5070 活跃 78.5% 时间、峰值 3543MB。

LaMa A/B 基准(scratch/bench_lama_gpu.py, 512x512)：
- Intel Arc(GPU.0) **0.225 s/片**；RTX 5070(GPU.1) **5.763 s/片** → **Arc 快 25.6x**
（同超分的结论：5070 走 OpenVINO 通用后端无原生 EP，Arc 有原生 Level Zero）

修复：inpainting_provider 同样改为 robust_performance(护盾=Arc) 时优先 GPU.0；5070 档仍走 GPU.1。
单元验证：LaMa robust_performance→GPU.0，5070档→GPU.1。未提交。
预期：LaMa ≈60片×(5.76→0.225s) 由 ~346s 降到 ~14s，引擎有望从 999s 降到 ~6min 量级。
注意：baseline 699s 那次 LaMa 未必真在 5070 慢路径上(可能回退CPU)，故基线本身也含未挖掘的 LaMa 成本。

## 自适应语义 Stage 1.4 主流程集成 + Stage 1.5 归档（09-12 下午）

把「自适应语义」从设计稿落到主流程，修复了多处以「能 import 但运行即崩/静默错」暴露的 latent bug：

**修复清单（按根因）**
1. **run_universal_engine.py 调用链全错**：`classify_material`→真实 `classify_material_family`；
   `classify_material(img, fp)`→`(fingerprint)` 单参；`CategorySelector` 类不存在→模块级 `select_categories`；
   `selector.select_categories(material_family, ...)`→真实 5 参签名。守卫改为 `locked`（默认）不进自适应块，preset 原样保留。
2. **engine/adaptive/__init__.py 导出未建模块** → 创建 **episode_archiver.py**（JSONL 追加归档 + load_episodes，
   线程安全，无需迁移 schema）。此 bug 会让 `import engine.adaptive` 直接 ModuleNotFoundError（连单测都跑不了）。
3. **webui/backend/api/adaptive.py 全面错**：`db_mgr.get_connection()`（无此方法）→ `db_mgr.execute()`；
   SQL 列名全错（引用 categories.name / version_id / base_confidence、affinity.category_name / a.confidence、
   category_prompts.prompt_text 等不存在列）→ 真实 schema（id/name_zh/name_en/affinity/prompt）；
   DB 默认路径 `.parent*3` 解析成 `webui/webui/data` → 应为 4 级到 project_root 再进 `webui/data`。
4. **main.py 漏注册** adaptive_router（grep 才发现）。
5. **🔴 fingerprint._apply_pca 致命 bug（sklearn 装好后才暴露）**：首次调用建「未 fit 的 StandardScaler」
   却立刻读 `_scaler.mean_` → AttributeError，所有指纹/材质/集成测试全崩。改为显式赋值 `mean_=zeros/scale_=ones`。
   之前冒烟测试「通过」是因为环境无 sklearn 走了降级分支——**印证：装齐依赖跑测试才能真正验代码**。
6. **🔴 材质判别 LAB 量纲 bug（隐蔽且影响全部分类）**：cv2 BGR2LAB 返回 L∈[0,255]、a/b∈[0,255]（中性=128），
   material_classifier 却当标准 LAB（中性=0）用 → 任何图 `bg_b≈128>20` 误触金地屏风规则、中性灰算成饱和度≈181。
   修复：fingerprint 把 median/mean/饱和度换算到标准 LAB（直方图保留 OpenCV 原值不扰动 PCA 原始特征）。
   修正后 4 类参考图全部判对；金地屏风 L*=80.0 边界把 `>80` 放宽为 `>=80` 使其置信度过 0.5 阈值。
7. **category_selector.py**：新增 `db_category_to_preset_class` + `merge_into_preset_format`（DB 类目→preset 兼容
   `name`/`prompt`；hybrid 保留 preset 元数据并去重）。关键事实：preset.ai_semantic_classes 命名空间 ≠ DB categories.id，
   bridge 是后续阶段工作，Stage 1 不强行映射。

**验证**
- 集成冒烟 `scratch/smoke_adaptive_integration.py`：locked 保持 preset；auto 以 DB Top-20 替换；hybrid 保留 2 类目+补 19=21，去重生效。
- **单测 23/23 全绿**（`tests/test_adaptive_fingerprint.py` + `test_adaptive_selector.py`，系统 Python 3.12 跑）。
- API 端点直调验证：`/api/adaptive/stats`（v0-seed/20 类/47 提示词）、`/categories?material_family=金地屏风`（20 类含 织锦边框/鸟禽/雁/印章）。

**依赖结论（已闭环）**：项目实际跑**系统 Python 3.12**（非托管 3.13）。系统 3.12 已含 numpy/cv2/scipy/skimage/torch，
仅缺 sklearn（已装 1.9.1）+pytest（已装 9.1.1）。「其他安装方式」= 往系统 3.12 装缺失包，绕开托管 3.13 装核心依赖持续超时。

**待办（未做）**：run_universal_engine 的预设需 `preset["mode"]="auto"/"hybrid"` 才触发自适应（默认 locked=不启用）；
真实图端到端（带 GroundingDINO/SAM2 全引擎）未在本次跑，因需重引擎+分钟级耗时。Stage 1.5 归档已在指纹/材质/选择后被调用点预留（run_universal_engine 暂未接 archive_episode，仅 import 可用）。

## Stage 1.5 收口：archive_episode 正式接入主流程（09-12 续）

上一节「待办」里唯一的松口——run_universal_engine 未接 archive_episode——现已闭合。

**改动（run_universal_engine.py 自适应块）**
- 导入区加入 `from engine.adaptive.episode_archiver import archive_episode`（同块内惰性导入，不拖慢 locked 默认路径）。
- 合并后新增第 5 步：从 `merged` 抽取类目 id（`c.get("category_id") or c.get("name")`），
  `merged` 非空→`outcome="selected"` 且更新 preset；空→`outcome="fallback_to_preset"` 保持原 preset。
- 两条分支都调 `archive_episode(material_family, category_ids, fingerprint=fp, image_path=input_path,
  outcome=..., confidence=mat_conf, notes=f"mode=...; n_selected=...")`，单独 try 包裹，归档失败不影响主流程。
- 归档落盘默认 `webui/data/adaptive_episodes.jsonl`（CWD=project_root），与 WebUI 词库同目录，供 Stage 2+ 消费。

**验证**
- 重跑单测 **23/23 全绿**（pytest_out2.txt），确认接入编辑无回归。
- 新增 `scratch/smoke_archive_episode.py`：合成图→指纹→材质判别→select(auto)→merge→archive→load_episodes 读回。
  结果：episode_id 落盘、`material_family`/`n_categories=19`/`outcome`/`fingerprint_summary{image_shape}` 全部正确读回；临时文件清理。
  （脚本断言曾误把 list 当 tuple 比致一次失败，已修正为「2-元组/正值」判定，非产品问题。）
- `py_compile run_universal_engine.py` 通过（COMPILE_OK）。

**Stage 1 全链路状态**：1.4 主流程集成 + 1.5 归档调用点均已落地并验证；剩余仅「真实图端到端带全引擎跑」（需分钟级耗时，非阻塞）。
preset 命名空间 ↔ DB id 桥接明确为后续阶段（Stage 2+）工作，Stage 1 不强行映射。