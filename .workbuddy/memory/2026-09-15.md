# 2026-09-15 工作日志

## 全量文档+代码深度审计（进度汇报）

对全仓库做了逐文件级深读审计（engine/core、engine/schemas、engine/operators、engine 顶层、engine/providers、engine/adaptive、tests、webui 全栈、presets、docs），产出结构化技术进度报告。

### 高价值结论（跨会话必备，勿被 HANDOFF 误导）

1. **自适应链路在生产默认下是死代码**：`run_universal_engine.py:223` 硬门 `if adaptive_mode in ("auto","hybrid")`，
   `adaptive_mode = get_adaptive_mode(preset)` 读 preset **顶层 `mode`**，6 个 preset **全部无该键** → 恒 `locked`。
   ⇒ Stage 1/2/4/5.2 引擎侧（指纹/材质/选类目/CBR/主动学习入队）**从不执行**。
   例外：Stage 3 影子学习（`:438-448`）与检出级入队（`:454-471`）在门**外**，默认会跑（但 Stage3 只 print 不落库）。

2. **HANDOFF.md 严重过时**（最后更新 2026-09-12，被 09-13/14 提交超越）：
   称"自适应仅 Stage1""PLATE 未打通"均与代码相反。以 README §六 / ARCHITECTURE §7 / 技术尽调 §十二 / adaptive-semantics/03 为准。

3. **生产真正生效的路径**：`run_universal_engine.run_pipeline`（6 步内存流式）→ `psb_builder`(适配层) → `core/psd_compiler`(唯一写盘)。
   `pipeline/01~06` 是**遗留平行实现，无任何调用者**（且全线硬编码像素坐标）。

4. **死代码/未接线清单**（已实测无调用者）：
   `core/geometry.py`、`core/lighting.py`（⇒ R2"detect_image 经平场"是空承诺）、`core/io_utils.py`(半死)、
   `operators/seam_harmonizer.py`(⇒ R3 未接线)、`operators/metallic_foil.py`、`operators/deocclusion_operator.py`、
   `engine/deocclusion.py`(死导入)、`engine/api.py`(孤岛)、`schemas/device_config.py`+`profile_config.py`(无消费者)、
   `webui/backend/core/background_learner.py`(孤儿，docstring 里的 trigger-learning/learning-status 端点**未注册/不存在**)。
   死常量：`models.LAYER_NAME_RE` / `PIPELINE_STAGES`。

5. **自适应缺陷（真实 bug）**：
   - `fingerprint.py` PCA 从未训练，`_pca_model="placeholder"`，embedding = 84 维原值 + 44 零。
   - `category_tree.inherit_priors()` → `NotImplementedError`（`category_priors` 空表）。
   - `migration_003` 有 `architecture` **自环** → `get_ancestors` 死循环；11 个不存在的叶子 id 被"假成功"日志。
   - **CBR 格式断裂**：检索找 `webui/data/episodes/**/*.episode.json`，归档写 `adaptive_episodes.jsonl`；目录不存在 ⇒ 永不命中。
   - `archive_episode` 未传 `audit_8d` ⇒ `audit_passed` 恒 False ⇒ 索引筛空。
   - `active_learner.identify_uncertain_categories` 字段错配（`dino_detections` 无 `confidence`、logits 名是 `logits` 非 `logit`）⇒ 检出级全部被判"不确定"。
   - `learner` old_weight 硬编码 1.0、无 clamp、docstring 称贝叶斯实为纯乘性。
   - `regression_tester` 名义 8 维实判 3 维；baseline `lost_ratio` 全 0.0。
   - `db_manager` 无"创建版本/写 snapshot"API ⇒ `category_versions` 恒只有 v0-seed。
   - `manifest.save(mask_dir)` 空壳，不写掩码 ⇒ R1 追溯链在落盘处断开。

6. **测试**：引擎 56 + 自适应 84 + golden 14 = 154（152 passed / 2 skipped）；WebUI 28。
   `test_adaptive_wiring.py` 用**源码字符串 assertIn** 做接线守卫（不验可达性，弱）；
   `test_golden_layers.py` 有 2 个 `skipTest`（理由"待 Stage4-5"已过时）。

### 自适应端到端修复收口（2026-09-15）

- 根因：`chinese_ink_landscape_ai` 的 preset 类目使用 `layer_name`，而自适应 `_attach_canonical_id()` 只读取 `name`，导致 11 个原有类目无法补规范 `category_id`，日志打印为 `None`，学习归因桥接不完整。
- 修复：`engine/adaptive/category_selector.py` 兼容 `name/layer_name/label_cn`，统一补 `name` 与真实 DB `category_id`；`run_universal_engine.py` 的检测回填同时按三种字段建映射；新增 Stage 2 回归用例。
- 水墨真实复测：`outputs/adaptive-e2e-20260915/case-04/retest3.psb` 出图成功；14 个类目 `none_names=0`，神经分割产出 8 个语义对象，Stage 3 产生 2 条归因，最终检测已写回 episode，8 维审计通过并准入 CBR。
- 验证：引擎全量 `208 passed / 2 skipped`；WebUI `34 passed`；关键自适应回归 `49 passed`；类目桥接/学习回归 `46 passed`。
- 测试环境：本机用户目录 YAPF 缓存清理会触发安全确认；复测通过项目内临时 `sitecustomize` 隔离 YAPF 缓存完成，临时 shim 已清理，原用户缓存未修改。

7. **文档矛盾（M1–M19）核心几条**：HANDOFF 过期；README §一 vs §六 三处口径；技术尽调 §一~§十一 vs §十二 整体反转；
   测试基线 64/128/152 三值；plan 与 MEMORY 的 ADR 状态相反；`target_w/h` 引擎从未读取（commit c8c6069 A 项）。

8. **硬件路由定调**（实测）：robust_performance = primary GPU.1(5070) + shield GPU.0(Arc)；provider **护盾 Arc 优先**。
   LaMa 512²：Arc 0.223s < CPU 1.072s < 5070 1.222s；RealESRGAN 跑 Arc；分割固定 CPU（sm_120 无 kernel）；NPU 空转。

9. **preset 完成度两极化**：`japanese_screen_gold`(250行，唯一含 density_refine/density_band_classes/blob_instances)、
   `textile_damask`、`western_oil_painting` 完整；`chinese_ink_landscape_ai` 完整但 schema 另类；
   `traditional_chinese_ink`/`commercial_illustration` 仅 11 行骨架。**6 个均缺顶层 mode/auto_evolve**。

---

## P0–P2 修复批次（用户选择"全部 18 项 + 真正启用自适应 + 全部接线"）

### B 组（缺陷修复，行为向设计意图收敛）
- ④ `cbr_retriever`/`episode_archiver`：归档 JSONL vs 检索 `episodes/**/*.episode.json` 双轨 → **双通道统一**；
  归档新增 `embedding`/`audit_8d`/`auto_tune`；新增 `load_episode_by_id` / `update_episode_audit` / `sync_index_from_log`。
- ⑤ `migration_003`：`architecture` 二级/三级同名 → `parent_id=self` 自环；改为**跳过自环 + 数据修复**，
  并改用 `cur.rowcount` 区分"未命中"与"成功"（消灭 11 条假成功日志）。已在真实库执行：残留自环 0，总 27/有父 13。
- ⑥ `active_learner.identify_uncertain_categories`：兼容 `dino_detections`（`layer_name`/`logits`/`boxes`），
  新增 `_max_logit()`；修复"confidence 缺失 → 全部检出被判不确定"。
- ⑦ `manifest.save(mask_dir)`：由空壳改为**真正写掩码 + 回填 `recon_mask_path`(relpath)**；新增 `attach_recon_mask`；
  `__post_init__` 挂 `_recon_masks`（不进 JSON）。引擎原已在 819 行写掩码（ASCII slug），二者不冲突。
- ⑩ `category_tree.inherit_priors`：`NotImplementedError` → **优雅跳过/有数据则复制**；`get_ancestors` 加**防环**。
- ⑭ `learner`：权重 clamp `[0.05, 5.0]`（含跨 episode 累乘）。
- ⑮ `regression_tester`：名义 8 维实判 3 维 → 实判 4 恶化型 + 层数 + 纯净性；`total_size_mb` 从 PSB 实体取；消除除零。
- ⑯ `db_manager.create_version()` 新增（此前版本链无创建入口）。
- ⑪ **核实已修**：`resolve_output_size` 早已 CLI>preset（仅文档滞后）。
- ⑫ `task_manager.PYTHON_BIN` → env `ULS_PYTHON_BIN` > `sys.executable` > 兜底。
- ⑱ `io_utils.imwrite_unicode`：未知扩展名默认 PNG（不再回落有损 jpg），支持 bmp/tif/webp。
- ③ **核实已修**：`codecs_accelerator.fast_compress_rle` 早已分块流式（CHUNK_ROWS=256）。

### C 组（改变 PSB 输出，需重跑金标准/RK-16）
- ① `japanese_screen_gold.json` 加顶层 `mode="hybrid"` + `auto_evolve=true`；
  `merge_into_preset_format` 加**限量补充护栏**（affinity≥0.6 且 ≤10 条，未提供 affinity 则不过滤，兼容测试）。
- ⑧ 新增 `_build_operator_ctx()`：`detect_image = LightingManager.normalize_illumination(...)`；
  `_run_plate_operators` 的 contour_protection / micro_holes 改传 ctx（R2 真正生效）。
- ⑨ `seam_harmonizer` 作可选前置步（preset `seam_harmonization.enabled`，返回键 `harmonized_image`）；
  `metallic_foil` 作可选印前算子（preset `plate_operators.metallic_foil.enabled`，产 13A/13B 专色层）。
  **注意**：`deocclusion_operator` 其实早已接线（Step 4），纠正此前"未接线"误判。
- ⑬ `background_learner` 增 `drain_once()`/`start_worker()` 守护线程消费队列；
  `api/adaptive.py` 注册 `POST /adaptive/trigger-learning` + `GET /adaptive/learning-status`（此前仅 docstring）。
- ⑰ 新增 `engine/schemas/preset_schema.py`；`load_preset` 非破坏校验（默认告警，`ULS_PRESET_STRICT=1` 升级错误）。
- 审计回填：`task_manager._backfill_episode_audit()` 从任务日志取 episode_id → 回填 audit_8d → 重建 CBR 索引。

### A 组
- `README.md` §一 状态表 + §速度览 更正（PLATE 已打通、自适应已启用、测试基线）；
- `HANDOFF.md` 加 **§9 修复批次**（最新状态优先阅读），§6 第 6 条改写，§7 测试数更新为 159。

### 验证
- 引擎 **159 passed / 2 skipped**（基线 152 + 新增 7：`tests/test_manifest_masks.py` 5 + tree 增 2）；
- WebUI **28 OK**；迁移 003 真实库执行成功；DB 已备份 `webui/data/adaptive_semantics.db.bak-20260915`。
- **未做**：前端无改动故未跑 `tsc`；C 组改变输出的端到端金标准/RK-16 复核需用户在本机跑（本环境无模型/大算力）。

---

## 端到端实测（2026-09-15，用户授权本机跑 --mode both）

命令：`run_universal_engine.py --input inputs/source_4000.jpg --preset japanese_screen_gold --mode both --dpi 150 --profile robust_performance --seed 42`
产物：`scratch/e2e_20260915/`（result.plate.psb / result.design.psb，日志 engine.log）

### 结果（修复前 hybrid：补充 10 个类目）
| 线 | 耗时 | 体积 | 层数 | Step1 分割 |
|---|---|---|---|---|
| PLATE | 474.5s | 1.80 GB | 43 | 225.29s |
| DESIGN | 399.9s | 1.53 GB | 43 | 139.51s |

- **① 生效**：两条线均打印 `[Adaptive] 自适应语义已启用: HYBRID`、材质判别（误判为"油画布" conf 0.55）、
  CBR 检索（无命中）、人审入队（2 个类目级 + 20 个检出级）、`类目已更新: 22 个（hybrid）`（12 preset + 10 DB）、
  episode 归档（JSONL）。Stage 3 影子学习 21 条归因/21 条建议（未落库）。
- **⑧ 生效**：PLATE 印前算子 `contour_protection: ok`，`cut_y_source=contour_protection`，
  未出现 "[R2] 照度平场不可用" 回退（说明 LightingManager 正常）。
- **⑨ 在位**：`metallic_foil: disabled`、`micro_holes: disabled`、`trapping: disabled`（按 preset 开关如实报告，未启用）。
- **R1/R6 合规**：PLATE `plate_purity_ok=true`、`generated_pixel_ratio=0`、TAC 329.8→300.0 已压制、有效源 37.5 PPI。

### 发现的真实缺陷（已修）
hybrid **补充了 10 个泛类（顶到 max_supplement 上限）**，其中多个与 preset 已有层**语义重复**：
- `calligraphy`→别名名 `题跋落款墨书_Calligraphy_Inscription` 与 preset 09B **完全相同**——
  根因：去重用 `name_zh`（"题跋书法"）而非**别名解析后的最终名**，故漏网。
- `brocade_frame`→`织锦边框_Brocade_Outer_Frame` 与 preset 的 10A **重复**——10A/10B 不在
  `ai_semantic_classes`（在 `layer_semantics.name_mapping`），故未被比对。
- `mountains_cliffs`/`trees_vegetation`/`figures_scholar`/`architecture` 与 preset 的
  04A/05A/07/06 **英文词元重叠**（cliffs/trees/scholar/architecture）。
- 后果：DINO 提示词近乎翻倍 → Step1 从 ~102s 涨到 225s；产物混入未编号层 `山石崖壁_Mountains_Cliffs`。

### 加固（category_selector.merge_into_preset_format）
① 去重移到**别名解析之后**（比对最终落盘名）；② 英文词元重叠视为语义重复；
③ 新增 `preset_full` 参数把去重语料扩展到**整个 preset 文本**（覆盖 10A/10B）；
④ `max_supplement` 默认 10→5；⑤ `run_universal_engine` 传入 `preset_full=preset`。
离线复现验证：补充数 **10 → 5**（仅剩 修复痕迹/云霭/瀑布/舟船/竹，均非重复概念）；引擎 159 passed 不变。

### 局限（待办）
- 材质判别把金地屏风判为"油画布"（conf 0.55）→ 亲和度查询走错家族，是 hybrid 灌入泛类的诱因之一。
- CLI 直跑不经 WebUI 审计 → `audit_passed` 恒 False → CBR 永远检索不中（只有 WebUI 路径有回填）。
- C 组改动后的产物耗时约 1.7×（PLATE 277→474s），需用户确认可接受或进一步限制 hybrid 补充。

### 加固后重跑（--mode design，同一输入）
- `类目已更新: 17 个（hybrid）` = 12 preset + **5 补充**（修复痕迹/云霭/瀑布/舟船/竹），**重复层 0**，
  层数 43→**42**（原先被灌入的 `山石崖壁_Mountains_Cliffs` 未再出现）。
- 但 5 个补充类目**一个都没产出图层**（DINO 未命中）→ 本次 hybrid 补充**纯属开销**（Step1 仍 ~158s）。
- 产物：1.49 GB / 16000×7808 / 148 层？实际 **42 层**；Step1 158.17 / Step2 26.20 / Step3 0.69 /
  Step4 43.69 / Step5 107.97 / Step6 51.38，端到端 **411.5s**。
- 结论：**①⑧⑨ 均已验证可用**；瓶颈与收益问题在**材质分类器误判**（金地屏风→油画布 0.55），
  导致亲和度查询走错家族、补充项无用。建议优先修分类器，或按 preset 收紧 min_affinity/max_supplement。
- 产物目录 `scratch/e2e_20260915/`（约 4.8 GB：plate 1.80 + design 1.53 + result2 1.49）可清理。

### A 方案落地：修材质分类器（金地）+ 收紧 max_supplement
**根因（实测）**：`fingerprint._extract_global_features` 的"背景"取自**外框 10%**，
而扫描件外框是博物馆深灰底（实测 source_4000.jpg 外框 **L37.3 a0 b0**），
画心才是金地（**L79.2 a5 b38**）。旧金地规则拿外框特征 → 恒失配 →
`油画布 0.55` 胜出。**这是 hybrid 灌入泛类的源头**。

**修改**：
1. `fingerprint.py`：新增中心主体区统计 `center_median_LAB` / `center_saturation`
   （去外框 15% 的 median/mean；**不加入 vector**，embedding 维度不变，向后兼容）。
2. `material_classifier.py`：**仅金地规则**改用中心区统计（其余三族维持原外框口径，零回归）；
   金地判据重标定为以 **b\*（黄度）为主**：b>30→0.40 / b>20→0.25 / b>10→0.10；
   L≥75→0.30；\|a\|<15→0.15；20<sat<55→0.15；edge 0.03–0.10→0.05。
3. `category_selector.merge_into_preset_format`：`max_supplement` 默认 **5 → 3**。
4. 新增回归测试 `tests/test_material_classifier.py`（5 例：金地/无 center 键回退/暗中心/纸白/缺字段）。

**验证**：source_4000.jpg → **金地屏风 1.00**（原油画布 0.55）；
其它 3 图维持原判（无双本/水墨山水仍判"绢本工笔"——属既有弱点，未引入回归）；
金地家族下 hybrid 仅补充 **3 个**（修复痕迹/梅花/竹，均非重复）；
引擎 **164 passed / 2 skipped**（159 + 5 新）。

**端到端最终复跑（--mode design，修复后）**：`材质判别: 金地屏风（1.00）`、`类目已更新: 15 个（hybrid）`
= 12 preset + 3 补充；产物 1.53 GB / 43 层；端到端 **394.4s**；
分步 Step1 **123.21s**（较修复前 158.17s ↓22%）、Step2 24.64 / Step3 0.68 / Step4 45.72 / Step5 121.64 / Step6 59.10。
（对照修复前：hybrid=10 时 PLAN 474.5s / DESIGN 399.9s、Step1 139.5–225.3s。）

### 追加项：宣纸水墨 / 绢本工笔 切中心区口径并单独标定
**动机**：金地修复后，两族仍用外框口径，导致"有设色的写意水墨"被判成"绢本工笔"。

**标定依据（实测中心区）**：
| 样本 | 中心 L / b* / edge / glcm | 期望 | 实得 |
|---|---|---|---|
| source_4000.jpg（金地） | 79.2 / 38.0 / 0.069 / 0.310 | 金地屏风 | 金地屏风 1.00 ✓ |
| 宋代写意山水画创作.jpeg（水墨） | 69.8 / 15.0 / 0.132 / 0.236 | 宣纸水墨 | **宣纸水墨 0.65** ✓（原绢本 0.70） |
| damask_sample.png（织物） | 73.3 / 13.0 / 0.203 / 0.337 | 绢本/织物 | 绢本工笔 1.00 ✓ |
| 生成类似风格商用图.png | 84.7 / 27.0 / 0.091 / 0.341 | 金地（偏金） | 金地屏风 0.90 ✓ |

**判据（区分点 = 笔触密度/纹理能量）**：
- 宣纸水墨：cL>60、cb<20、edge<0.16、glcm<0.28（稀疏写意）；
- 绢本工笔：cb 10–30、65<cL<88、**edge≥0.16 或 glcm≥0.28**（细腻密集）。
- 油画布仍用原外框口径（未标定，避免回归）。

**测试**：引擎 **166 passed / 2 skipped**（新增 2 例：《写意水墨→宣纸水墨》《密集细腻→绢本工笔》）。
新增签名：`_score_ink_paper(cL, cb, sat, edge, glcm)` / `_score_silk_painting(cL, cb, sat, edge, glcm)`。
**局限**：无绢本真值样本 → 绢本阈值属保守估计，待补样本再校。

### 继续收口复核（2026-09-15 22:16）
- 复核 Git 工作区：`main` 干净，HEAD 为 `d7f2f74`，前一提交为 `fb1a618`。
- 定向运行 `tests/test_episode_archiver_finalize.py` 与 `webui/backend/tests/test_audit_backfill.py`：**11 passed**。
- 已确认严格 8 维准入、`audit_full` 原始报告保留、DESIGN 豁免第 ⑦ 维，以及 both 模式按 plate/design 产物分别回填 episode 均有测试覆盖。
- 将历史残留任务 13、18、19 状态同步为 completed；本次无新增代码改动。
**未复跑端到端**：本项不影响金地 preset 的家族判定（仍金地屏风），故无需再跑 7 分钟。

---

## A 类收尾（2026-09-15）

- **A3 清理**：删除 `scratch/e2e_20260915/`（6507 MB / 124 文件）与本次全部临时脚本与日志；
  `scratch/` 中**既有**的 test_*.psb 等大文件未动。（注：环境 `safe-delete` 包装器对部分文件报
  `trash-failed`，但终态校验 remaining=0，已实际移除。）
- **A2 RK-16 复核**（生产配置 `--scale` 默认 4.0，PLATE 同 seed 双跑）：
  - PSB MD5 两次完全相同：`906357c2819f32307c1cc06ea1e2f791` → **逐字节一致**；
  - 27 个重建掩码 PNG 逐像素一致（交集 27，差异 0）；
  - 8 维交付审计门 **8/8 通过**：43 层 / duplicate_names 0 / res_mismatch 0 / lost_px 0 /
    rmse_raw 26.51 & lowfreq 19.69 / 底板 opaque 100% / TAC 300.0 & plate_purity_ok & K 99.5% /
    manifest 43==43。
- **A1 提交**：`9ce7dd5`「fix(adaptive)+feat: P0–P2 修复批次 + 自适应机制启用（2026-09-15）」
  26 文件 +1721/−195；新增 `engine/schemas/preset_schema.py`、`tests/test_manifest_masks.py`、
  `tests/test_material_classifier.py`。
  - 提交前修复 **README 行尾翻转**（工作区 CRLF vs HEAD LF → 已改回 LF，消除 240 行伪改动）；
    另清理 2 处真实尾随空格（category_tree/learner）与 1 处 EOF 空行。
  - 仓库为**本地 main、无远端** → 仅本地提交，无 push；`git fsck --full` 干净（仅 dangling blob 警告）。
  - 提交后工作区仅剩 3 个**有意排除**的未跟踪文件：`inputs/宋代写意山水画创作.jpeg`、
    `inputs/生成类似风格商用图.png`、`profiles/CoatedFOGRA39.icc`（均非本次改动）。
- **认知更正**：本项目**没有**"源码/构建副本/ZIP 三处同步"的发布约定（"打包分发"在尽调报告里
  仍是未完成的 P2 待办）；此前把另一个项目的交付纪律误套过来了。

---

## B/C 组收尾（2026-09-15 下午）

- **B1 启用 R3 + 金属分色**：`presets/textile_damask.json` 新增 `seam_harmonization`（cyclic_vertical）
  与 `plate_operators.metallic_foil`（valid_mask_layer=金箔卷草层）。
  实跑 damask：`[R3] pre_rmse=21.461 → post_rmse=15.588`（**↓27%**）；`contour_protection/micro_holes/metallic_foil = ok`，
  `trapping: skipped`（该 preset 配的专色层"02_巴洛克团花"未产出，如实报告——**旧有配置问题，非本次引入**）。
- **B2 端点真机验证**：新增 `webui/backend/tests/test_adaptive_learning_endpoints.py`（3 例，TestClient），
  覆盖 `GET /adaptive/learning-status`、`POST /adaptive/trigger-learning`（无 episode → 404；有 episode → 入队且状态可见）。
- **B3 回填链路验证**：新增 `webui/backend/tests/test_audit_backfill.py`（2 例）；日志实证"索引重建 1 条"。
- **B4 版本链接线**：`apply_feedback_to_category` 在人审改动后调用 `db_manager.create_version(...)` 并激活；
  失败不影响反馈（回传 `version_note`）。新增 `tests/test_feedback_version.py`（3 例）。
- **B5 CLI 冷启动闭环**：引擎新增 `ULS_AUDIT_AFTER_RUN=1` → 出图后跑 `tools/audit_psb.py` → `update_episode_audit` → `sync_index_from_log`；
  新增模块全局 `LAST_EPISODE_ID`。实跑 japanese(scale1)：`[PostRun] 8 维审计门 exit=0` + `CBR 索引已按真实审计重建：9 条`。
  另：`ADAPTIVE_EPISODE_PATH` / `ADAPTIVE_PENDING_PATH` / `ADAPTIVE_INDEX_PATH` 支持**调用时**解析（测试隔离）。
- **C1 结论**：05A/04B 由**区域先验 SAM** 解决、04A 由**密度精修**恢复、03 由**密度带**产出 → **无需新增算法/Frangi**；
  仅 04D 确认不可自动（数据条件）。见 `docs/未决提取难题结论_20260915.md`。
- **C2 品类验证**：金地✅、**壁布✅**（exit=0）、**水墨✅**（172.8s / 9 层）；烫金=金地变体（覆盖）；
  **油画缺样本**（未验，待补图）。
- **C3 油画布中心区化**：四族口径统一（阈值保守沿用，代码标注"未标定"）。
- **C4 文档勘误**：新增 `docs/文档与代码对齐勘误_20260915.md`（M1–M19，**不改历史文档**）。

**测试**：引擎 **169 passed / 2 skipped**；WebUI **33 OK**。
**踩坑记录**：PowerShell 的 `*>>` 重定向会把中文日志弄成 GBK/UTF-8 乱码，且对写 stderr 的原生命令会
误报 `exit=1`（NativeCommandError）；**要准确捕获引擎输出与退出码，改用 Bash 原生 `>` 重定向**。
另：本环境 Bash shim **缺 coreutils**（`dirname`/`mkdir`/`tail`/`head` 均无）→ 建目录等操作用 PowerShell。

### 补充：用户追加了样本 → C2/C3 缺口补齐（2026-09-15 14:20+）
`inputs/` 新增 `油画.jpeg`、`明代写意山水画创作.jpeg`、`元代.png`、`生成类似风格商用图--1.png`。

**7 张样本实测判定（修正后 7/7 合理）**：
| 样本 | 中心 L/a/b, sat, edge, glcm | 判定 |
|---|---|---|
| source_4000.jpg | L79.2 a5 b38 sat34.9 edge0.069 glcm0.310 | 金地屏风 1.00 |
| 生成类似风格商用图--1.png | L89.0 a0 b31 sat34.4 edge0.123 glcm0.254 | 金地屏风 1.00 |
| damask_sample.png | L73.3 a2 b13 sat14.0 edge0.203 glcm0.337 | 绢本工笔 1.00 |
| 宋代写意山水画创作.jpeg | L69.8 a1 b15 sat14.5 edge0.132 glcm0.236 | 宣纸水墨 0.65 |
| **明代写意山水画创作.jpeg** | L84.3 a0 b13 sat14.1 edge0.123 glcm0.325 | **宣纸水墨 0.65**（修正前误判绢本 0.75） |
| 元代.png | L92.5 a0 b13 sat13.1 edge0.052 glcm0.750 | 宣纸水墨 0.75 |
| **油画.jpeg** | L52.2 a7 b27 sat28.1 edge0.241 glcm0.159 | **油画布 0.70** ✓ |

**判据修正（关键）**：绢本工笔的 **glcm 稠密项改为以 `edge_density ≥ 0.16` 为前提** ——
否则"明代水墨"（glcm0.325 高、edge0.123 低）会被误判为绢本。织物 damask（edge0.203）仍稳判绢本。

**C2 补齐**：油画品类端到端实跑 `oil 218.6s / 8 层 / exit=0`（western_oil_painting, design）。
→ **4/5 品类实跑通过**；烫金=金地变体（覆盖）。仅绢本工笔**无真值样本**。
**C3 补齐**：油画布阈值已用真值样本标定（不再是"未标定"）。
**顺带**：油画/水墨 preset 的 8 维审计门为 `exit=2`（新品类阈值未校准，非崩溃）。
测试：引擎 **171 passed / 2 skipped**（+2：《明代→宣纸水墨》《油画→油画布》）。

### 项目整体状态汇报核对（2026-09-15）
- 最终回归：引擎 **209 passed / 2 skipped**，WebUI **34 passed**；关键自适应回归、类目桥接和学习闭环均通过。
- 水墨字段兼容修复后的真实证据：`outputs/adaptive-e2e-20260915/case-04/retest3.psb`，14 个类目无 `None`、8 个语义对象、Stage 3 归因 2 条、8 维审计通过并准入 CBR。
- 九样本首轮双跑证据：水墨 3 个 repeat 均真实命中 CBR；油画 preset 修复后独立 cold/repeat 均 8 维通过，repeat 命中 CBR；生产库未变化。
- 绢本工笔两张真实样本均正确分类为“绢本工笔”，但 ④内容承载与 ⑤合成等价性仍失败，属于算法质量问题，不通过放宽阈值掩盖。
- 已形成提交 `fb1a618`（`收口自适应语义与样本审计回归`），提交后工作树干净；未生成打包副本或 zip。
- 当前剩余风险：绢本工笔分层质量修复、`category_priors` 缺真实统计时仅优雅跳过、打包与依赖锁定仍未完成。

