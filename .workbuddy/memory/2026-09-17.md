# 2026-09-17 工作日志

## 启动 WebUI 供人工手动测试（00:05）
- 后端：webui/backend 下 `python -m uvicorn main:app --host 127.0.0.1 --port 8099`；
  前端：webui/frontend 下 `npm run dev -- --host 127.0.0.1`（vite 5173）。
- **坑**：vite 默认只绑 ::1，按 127.0.0.1 探测不可达 → 已在 vite.config.ts 的
  server 段固定 `host: "127.0.0.1"`（与后端 IPv4-only 保持一致）。
- **坑**：后台/子进程会随命令轮次结束被回收 → 需用常驻后台任务（run_in_background）
  方式启动服务，不能靠一次性 Popen 后返回。
- 长跑引擎任务须带 `CODEBUDDY_SAFE_DELETE_ENABLED=0`（safe-delete 钩子会杀长跑进程）。


## 选项 B 执行：撤销测试性 Auto-Tune 采纳（09:32）
- 还原 presets/japanese_screen_gold.json 至 HEAD（移除测试点击写入的 3 条密度带），
  工作区恢复干净；采纳内容备份于 scratch/autotune_adopted/*.patch（随时可恢复）。
- 后续若真要采纳：恢复 patch → 跑屏风金标准+16K 验证 8 维 → 有证据再合入。

## 样本重命名 + 家族体系 v2 + T1 训练管线（提交 c322a1d，push 待网络恢复）
- inputs/ 12 张全部逐张目验后 git mv 重命名（命名=材质家族_内容_形制）；
  引用同步 34 处 + glob/f-string 模式修正（test_material_classifier、
  test_sample_panel、test_upload 的 f-string 循环改显式列表）。
- 家族体系 v2 落盘 engine/adaptive/material_taxonomy.json：青绿设色独立成族
  （用户拍板）、西洋绘画暂合族（油画+湿壁画）、织物拆实物照/数码稿。
- 新增 tools/train_material_classifier.py（--validate-only/--train/--save）：
  语料校验（每族≥15 张目标）→ sklearn GBoost 留出 20% → 与规则判别器同集对比
  （不优不上线）。读图必须 imread_unicode（P0 门禁拦截 cv2.imread 后修正）。
- inputs_training/<家族>/ 训练语料目录（gitignore，本地投喂）：7 类种子 12 张已就位。
- ⚠️ 网络问题：github.com:443 直连被重置，push 暂失败（c322a1d 在本地），
  待网络/代理恢复后 git push 即可。

## 交付两份文档（提交 f76f03e，走 7892 代理推送成功）
- docs/训练样本补充清单与生图提示词_20260917.md：9 个新增品目（P1 误判类 5 /
  P2 负样本 4）+ 织物扩样模板，每类含理由与变量矩阵式生图提示词；来源纪律 7:3。
- docs/新类别自动学习接入方案_v1.md：六阶段（拒识标记→人审→增量触发→训练评测
  →版本化发布→灰度/回滚/容错），接口与文件契约汇总，M1-M3 分期。
- 网络备忘：本机代理为 127.0.0.1:7892（注册表 ProxyServer），git 需
  -c http.proxy=… 显式指定（不读系统代理）。

## 57 张新增样本精确归类 + 84 维重训（提交待推）
- 拼板目验 57 张：发现 5 处名实不符——`(9).jpeg` 名为书法实为工笔竹雀；
  `青绿设色1/5/12` 实为工笔重彩花鸟（玉兰锦鸡/荷花鸳鸯/孔雀牡丹）→ 归 绢本工笔；
  裸名 10/11/12 实为西洋油画名作（梵高咖啡馆/野兽派/立体主义）→ 西洋绘画。
- 归类后语料 158 张 13 族：纸本水墨 15✓ 绢本工笔 21✓ 西洋绘画 15✓ 其他 28✓ 达标；
  青绿 12 / 数码稿 13 / 金地 13 / 实物照 10 / 素描 10 接近；书法 3 唐卡 3 水彩 7 木刻 8 缺。
- 84 维特征重训：holdout 0.625（32 张小测试集）/ 全量 0.924 vs 规则 v1 0.044
  （v1 只认 6 族）。门禁未达（holdout<0.95）→ 不发布，符合方案。
- 训练脚本改用 fp[raw_features] 完整 84 维（此前拍平丢特征只有 10 维）。