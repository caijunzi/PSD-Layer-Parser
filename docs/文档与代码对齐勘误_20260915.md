# 文档与代码对齐勘误（2026-09-15）

> 本文件是**追加性勘误**，不修改任何历史文档原文（历史快照保持可追溯）。
> 凡本文件与旧文档冲突处，**以本文件 + 当前代码为准**。
> 所有结论均基于 2026-09-15 的**代码实测**（非文档转述）。

## 一、已确认的文档—代码矛盾（按影响排序）

| # | 文档与位置 | 原文主张 | 代码事实（实测证据） | 处理 |
|---|---|---|---|---|
| M1 | `docs/技术尽调与代码审查报告_20260910.md` §十二 前 | `codecs_accelerator.fast_compress_rle` 双份全量数据，16GB 机器**必然 OOM** | **已分块流式**（`CHUNK_ROWS=256`，`_write_chunk` 逐块写盘，内存峰值 ~8MB），`lengths` 仅 46KB | 文档过期；代码无需改 |
| M2 | 同上 / 计划 §5 | CLI `--scale` 被 preset 无条件覆盖，CLI 形同虚设 | `resolve_output_size` 已按 **CLI > preset.super_res_scale > 4.0** 实现 | 文档过期；代码无需改 |
| M3 | `README.md` §一双产品线 | PLATE 线 🔴 **未打通**（算子均未接入生产链路，实测产物为 RGB） | PLATE 已打通：真 CMYK + ICC FOGRA39 + 黑版 + TAC≤300% + 纯净性自动校验；算子链已接线（2026-09-15 起含 seam_harmonizer/metallic_foil 可选） | **已在本批更正 README** |
| M4 | `HANDOFF.md` §6 第 6 条 | 自适应机制仅 Stage 1 完成，**Stage 2/3/4/5 未做** | Stage 1–5.3 代码均已落地，且 2026-09-15 起**真正启用**（preset 顶层 `mode=hybrid`） | **已在本批更正 HANDOFF §9** |
| M5 | `docs/adaptive-semantics/01-architecture.md` | 规划建 `episodes` 表；人审用 WebSocket/SSE | 实际 `episodes` **表不存在**，走 `webui/data/adaptive_episodes.jsonl`；人审走 **前端 3s 轮询** `GET /api/adaptive/pending-feedbacks` | 以 `03-implementation-plan.md` 偏差注记 + ADR-021/022 为准；本文再次确认 |
| M6 | `docs/技术尽调与代码审查报告_20260910.md` 完成度表 #14 | **无 `.git`**（4.8GB 产物无回滚点） | 仓库已初始化（`main` 分支，本地无远端；最近提交 `cd7e2c3` → `9ce7dd5`） | 文档过期 |
| M7 | `engine/adaptive/fingerprint.py` 注释与计划 | 指纹为 **PCA 128 维** | 原始特征仅 **84 维**（55+24+5），PCA ≤84 维在数学上不可能；实际为 **84 维标准化后零填充到 128** | 已在代码 docstring 标注"占位；非真 PCA"；计划措辞待修（本文记录） |
| M8 | `engine/adaptive/category_tree.py` docstring | `inherit_priors` 标记 TODO，抛 `NotImplementedError` | 已改为**优雅跳过**（priors 表空时返回状态字典）并在有先验时复制 | 2026-09-15 已修（提交 `9ce7dd5`） |
| M9 | `engine/adaptive/db_manager.py` | 文档宣称具备"版本管理（激活/回滚/GC）" | 实际**缺"创建版本"入口**；`category_versions` 只能靠迁移写入 → 版本链空转 | 已补 `create_version()` 并**接线到人审反馈**（B4） |
| M10 | `webui/backend/core/background_learner.py` docstring | 宣称提供 `POST /api/adaptive/trigger-learning` + `GET /api/adaptive/learning-status` | 两端点**从未注册**，且 `task_queue` 无消费者 → 任务永挂 queued | 已补端点 + 守护线程消费者（B2） |
| M11 | `webui/backend/core/task_manager.py` | 审计在出图后运行，结果落 `result.audit.json` | 审计结果**未回填 episode** → CBR 的 `audit_passed` 恒 False、检索永远筛空 | 已补 `_backfill_episode_audit` + `sync_index_from_log`（B3/B5） |
| M12 | `run_universal_engine.py` 自适应块 | 归档 episode 供 Stage 2+ 消费 | 归档写 JSONL，而 CBR 检索找 `episodes/**/*.episode.json` → **格式断裂**，检索恒空 | 已统一为双通道（提交 `9ce7dd5`） |
| M13 | `migrations/migration_003_build_tree.py` | 迁移会打印"✅ 更新"即成功 | 存在 `architecture` **自环**（二级/三级同名）与 11 条**未命中却打印成功**的假日志 | 已修（跳过自环 + `rowcount` 校验 + 数据修复） |
| M14 | `README.md` §一 品类表 | 5 类中 2 类"✅ 端到端验证通过" | 2026-09-15 实测：**4/5 品类实跑通过**（金地 / 壁布 / 水墨 / 油画）；烫金=金地 preset 变体（覆盖）。绸本工笔**无真值样本** | 本文更新（原"仅 1 类"结论已过时） |

## 二、仍未解决（保持现状，不与文档冲突）

| # | 事项 | 现状 |
|---|---|---|
| M15 | 打包与分发（尽调 P2-2） | 仍**未做**：无 `pyproject.toml`/依赖锁定，仅 2 个 `.bat`。**本项目不存在"源码/构建副本/ZIP 三处同步"约定** |
| M16 | `third_party/`（GroundingDINO/sam2，140MB） | 仍 vendored（应改 submodule/pip），已 gitignore |
| M17 | 前端 `tsc --noEmit` | 本批未跑（无前端改动）；历史基线亦无 CI 门禁 |
| M18 | `profiles/CoatedFOGRA39.icc` | 误留未 gitignore；且其再分发许可需自行核实（见 `profiles/README.md`） |
| M19 | `docs/` 内部旧规划文档（v1/v2.x 并存） | 保留归档，不做逐篇重写；以 `UNIVERSAL_LAYER_ENGINE_DEVELOPMENT_PLAN.md` + 本勘误为准 |

## 三、口径约定（避免再次漂移）

1. **代码是唯一真相**：文档与代码冲突时，先跑一次实测再判定（本文件所有 M 项均如此）。
2. **历史文档不改**：修正以**追加新文档**形式进行（本文件即首例）。
3. **断言锚点**：`tests/` 中的回归（166+ 用例）+ `tools/audit_psb.py` 8 维审计门 是"文档声称已实现"的验收凭据。
