# 文档对齐清单（2026-09-09）

> 本次对齐依据：**`UNIVERSAL_LAYER_ENGINE_DEVELOPMENT_PLAN.md` v2.2**（单一真相来源）
> 冲突处理原则：以最终确认的修正内容为准；过期表述删除或加废止标记；不确定项保留并标记待确认，**不凭记忆重建**。

---

## 一、阻塞项（需先确认，再进入 M0）

| 项目 | 状态 | 说明 |
| :--- | :--- | :--- |
| 工作区文件缺失 | ⛔ **阻塞** | `src/`（整个前端，9 个 JS）、`scripts/`（server.py / serve.bat / verify.py）、`index.html`、`双击运行.bat`、`package.json`、`engine/cli.py`、`outputs/` 成品 PSD（1.2 GB）在 2026-09-09 22:55 复核时**均不存在** |
| 可恢复性 | 已排查 | 无 `.git`、回收站为空、父目录无副本 → **当前无法恢复** |
| 已采取措施 | 完成 | 在计划 §4.2 标 `‼️缺失`，§0.1 图例新增 `‼️`，并插入"工作区现状告警"章节；**未重建任何代码文件** |
| 待用户确认 | ❓ | 是有意移除（如暂缓前端）还是误删？据此决定里程碑顺延或先恢复 |

---

## 二、已更新文档

### 1. `docs/UNIVERSAL_LAYER_ENGINE_DEVELOPMENT_PLAN.md`（主文档，v2.0 → v2.2）

| 修正条目 | 同步状态 |
| :--- | :--- |
| 版本号与变更说明更新至 v2.2（含 SAM2/GroundingDINO/torch 接入决策） | ✅ |
| §0.1 状态图例新增 `‼️缺失` | ✅ |
| §1.2 基线表补实测行（放大倍率、有效源 PPI、1.2 GB、bbox、alpha） | ✅ |
| §1.2.1 **新增** 第二条业务线（书画元素拆解，14 层 @16000×7808） | ✅ |
| §1.3 新增 G7（可编辑元素层）、G8（生成受控可追溯） | ✅ |
| §1.4 非目标改写：明确不承诺任意图全自动、默认不引扩散模型、主包不含 torch | ✅ |
| §1.5 铁律「五大」→「六条」，R1 加重建区条款，新增 R6 层合成等价性 | ✅ |
| §2.1 选型表新增 SAM2 / GroundingDINO / 推理运行时 / 抠图细化 四行 | ✅ |
| §2.2 新增 ADR-010~015，ADR-012 按 v2.2 修订 | ✅ |
| §4.2 目录树：新增 `models/`、`requirements-ai.txt`、`fetch_models.py`；`pipeline/`、`masks_16k/` 由 ⛔ 改 ⏳ | ✅ |
| §4.2 **工作区现状告警章节 + `‼️缺失` 标记**（本次对齐新增） | ✅ |
| §6.2 算子清单新增 element_segmenter / depth_sorter / deocclusion | ✅ |
| §6.6 双产品线 PLATE / DESIGN（v2.1 新增） | ✅ |
| §6.7 元素层硬规格（v2.1 新增） | ✅ |
| §6.8 生成补全量化边界（v2.1 新增） | ✅ |
| §6.9 分割 Provider + 五档降级矩阵 + 分块推理 + 模型分发（v2.2 新增） | ✅ |
| §7.4 性能预算补 AI 推理（GPU/CPU/显存） | ✅ |
| §8.4 新增 V4-33~39 | ✅ |
| §10 新增 RK-10~17 | ✅ |
| §11.2 新增 M6、M7；§11.3 甘特补 M6/M7；周期 8 周 → 10 周 | ✅ |
| 附录 A 术语表补 工艺版层/元素层/重建区/层合成等价性 | ✅ |
| 附录 C 补 8 项代码改造 TODO | ✅ |

### 2. `docs/MEMORY.md`（新建）

| 内容 | 同步状态 |
| :--- | :--- |
| 六条铁律（R1–R6）全文 | ✅ |
| ADR-001 ~ ADR-015 完整记录 | ✅ |
| 实测基线表（16 项，含 10.95 PPI、1.2 GB、bbox、alpha、8 bit） | ✅ |
| psd-tools 1.19 API 校准表（正确/错误对照） | ✅ |
| 历史踩坑 5 条 | ✅ |
| 待确认项（工作区缺失） | ✅ |
| 许可核对清单（SAM2 / GroundingDINO / ICC） | ✅ |

### 3. `docs/DAILY_LOG.md`（新建，更新日志）

| 内容 | 同步状态 |
| :--- | :--- |
| 2026-09-09 全天演化（审核 → 实测校准 → v2.0 → v2.1 → v2.2 → 现状复核 → 对齐） | ✅ |

### 4. `GEMINI.md`（重写）

| 修正条目 | 同步状态 |
| :--- | :--- |
| 降级为**入口指针**，指向 docs/ 三件套，避免与 MEMORY.md 重复 | ✅ |
| 删除 "100% 物理真实复原" | ✅ 已列入废止表 |
| 删除 "0.0 误差保留真实光学反光" | ✅ 已列入废止表 |
| 更正 "CMYK 4 通道 32 位深" → 实测 **8 bit** | ✅ 已列入废止表 |
| 更正 "150.0 DPI" → **150 PPI** | ✅ 已列入废止表 |
| 裁切安全线 Y=718 → 改为算子运行时计算 | ✅ 已列入废止表 |
| 错位 100 px / 80 px 口径不一 → 标注以实测为准 | ✅ 已列入废止表 |
| 新增六条铁律摘要、双产品线、AI Provider 说明 | ✅ |
| 新增"停止条件"5 条（含 pipeline/ 不得判废弃） | ✅ |

### 5. `docs/UNIVERSAL_LAYER_ENGINE_DEVELOPMENT_PLAN_技术审核报告.md`（修订）

| 修正条目 | 同步状态 |
| :--- | :--- |
| P0-3 加删除线 + 【已被 §7.1 推翻】标记 | ✅ |
| P1-6 加【部分已被 §7.2 修正】标记 | ✅ |
| 结论速览表加对齐修订说明（实际 P0 = 2 项） | ✅ |
| 声明正文描述对象为 v1.0，旧表述为原文引用 | ✅ |
| §7 修订说明（P0-3 勘误、P1-6 勘误、分辨率真实性新发现、API 校准） | ✅ 已存在于上一轮 |

### 6. `outputs/图层三问答复.md`（工作区侧答复稿）

| 修正条目 | 同步状态 |
| :--- | :--- |
| "不需要 PyTorch" 结论被 v2.2 反转 → 加修订块声明以 v2.2 §6.9 为准 | ✅ |
| 保留有效部分（图层两种含义、家装工装分类、生成补白解释） | ✅ |

### 7. 工作区记忆 `.workbuddy/memory/2026-09-09.md`

| 修正条目 | 同步状态 |
| :--- | :--- |
| 错误的 P0-3 条目（psd-tools 写入存疑）→ 标记推翻并更正为 pytoshop | ✅ |
| 补记分辨率真实性、v2.1、v2.2、全量对齐动作、未做项说明 | ✅ |

### 8. 技能 `~/.workbuddy/skills/tech-doc-audit-rewrite/SKILL.md`

| 修正条目 | 同步状态 |
| :--- | :--- |
| 阶段 1 新增"扫全量目录找第二条业务线"必测项 | ✅ |
| 新增三条教训（pytoshop 误判、pipeline/ 误判为废弃、成品实测暴露空层缺陷） | ✅ |

---

---

## 二之二、2026-09-10 追加（工程配置与文档矩阵补齐）

| 文档 / 文件 | 动作 | 修正条目 | 状态 |
| :--- | :--- | :--- | :--- |
| `requirements.txt` | **新建** | 主依赖，版本按 2026-09-10 实测（numpy 2.4.6 / opencv 5.0.0.93 / Pillow 12.2.0 / psd-tools 1.19.0 / pytoshop 1.2.1），明确禁止 torch | ✅ |
| `requirements-ai.txt` | **新建** | torch / torchvision / onnxruntime + sam2、groundingdino（注释待按官方说明填写） | ✅ |
| `requirements-dev.txt` | **新建** | pytest / pytest-cov | ✅ |
| `.gitignore` | **新建** | 排除 outputs / intermediate / models / checkpoints / 大文件 / Python 缓存 | ✅ |
| `profiles/README.md` | **新建** | ICC 清单、获取渠道、许可记录要求、校验命令 | ✅ |
| `README.md` | **重建**（原文件已丢失） | 对齐 v2.2：双产品线、六条铁律、安装运行、目录速览、**当前状态与缺失告警**、已知缺陷 4 条 | ✅ |
| `docs/ARCHITECTURE.md` | **新建** | 部署视图、依赖方向规则（5 条）、AI 运行时探测流程、双产品线数据流、实测环境基线、性能预算 | ✅ |
| 计划 §2.1 选型表 | **修正** | OpenCV `headless 4.x` → 实测 `opencv-python 5.0.0.93`；Pillow `11.x` → 实测 `12.2.0` | ✅ |
| 计划 §4.2 目录树 | **同步** | requirements*.txt / .gitignore / README / ARCHITECTURE / MEMORY / DAILY_LOG / 对齐清单 标记为 ✅；profiles 改 ⏳ | ✅ |

## 三、有意保留 / 未改动（避免重复与脏数据）

| 文档 | 决定 | 理由 |
| :--- | :--- | :--- |
| `docs/archive/UNIVERSAL_LAYER_ENGINE_DEVELOPMENT_PLAN_v1_20260909.md` | **保留不动** | 历史存档，用于追溯；已在主文档标注存档位置 |
| `docs/ARCHITECTURE.md` | **不创建** | 内容已完整覆盖于计划 §4（分层架构、目录结构、契约卡、数据流），另建会产生重复与不一致 |
| `presets/*.json`（5 个） | **不改** | 属代码改动，按"先对齐不开发"的要求推迟；计划 §6.4 已定义目标 schema |
| 缺失代码文件 | **不重建** | 无可靠来源，凭记忆重建即制造脏数据 |

---

## 四、术语统一（已全文执行）

| 统一后 | 取代的旧写法 |
| :--- | :--- |
| PPI | DPI（描述图像时） |
| 通道墨量百分比 | 网点成数（连续调文件无网点） |
| 逻辑墨量（0=0% 墨） | 未声明量纲的 CMYK 数组 |
| 有效源 PPI | 与文件头 PPI 混用 |
| 工艺版层 / 元素层 | 笼统称"图层" |
| 重建区 `_RECON` | 未标记的补全内容 |
| 六条铁律 | 五大铁律 |
| 微孔 / 冲孔（perforation） | 打孔点阵等多种叫法 |

---

## 四之二、2026-09-10 开源调研与路线裁决（v2.3）

| # | 动作 | 落点 | 状态 |
| :--- | :--- | :--- | :--- |
| 1 | 调研 Github 现成方案（Qwen-Image-Layered / Stratum / 印前分色类 / LayerDiffuse） | 本轮对话 | ✅ |
| 2 | 路线裁决：A 自建内核 + 可选接入 | 用户确认 | ✅ |
| 3 | 计划新增 §6.10 开源参考与拿来主义边界 | 主文档 | ✅ |
| 4 | 新增 ADR-016（Qwen 仅 DESIGN 线）、ADR-017（陷印算子）、ADR-018（前端 manifest 契约） | 主文档 ADR 表 | ✅ |
| 5 | §6.9 补 `QwenLayeredProvider` + 5 条约束 + 降级矩阵 2 行 + preset 枚举 | 主文档 | ✅ |
| 6 | `MEMORY.md` 新增 §3.1 三条硬边界 + 许可清单补 Qwen / Stratum | MEMORY.md | ✅ |
| 7 | `DAILY_LOG.md` 追加 2026-09-10 章节 | DAILY_LOG.md | ✅ |
| 8 | 陷印算子、前端 manifest 契约实现 | 代码 | ⬜ 待实现（M1/M2） |

---

## 五、复核方式

```bash
# 1. 确认文档齐备
ls docs/  # 应有 DAILY_LOG.md / MEMORY.md / 文档对齐清单_20260909.md
          #         UNIVERSAL_LAYER_ENGINE_DEVELOPMENT_PLAN.md / 技术审核报告 / archive/

# 2. 确认主文档为 v2.2
grep -n "v2.2" docs/UNIVERSAL_LAYER_ENGINE_DEVELOPMENT_PLAN.md

# 3. 确认缺失项已标记
grep -n "‼️" docs/UNIVERSAL_LAYER_ENGINE_DEVELOPMENT_PLAN.md

# 4. 确认过期表述仅出现在"废止表/错误写法列"中
grep -rn "0 误差\|32 位深\|100% 物理真实" --include="*.md" .
```
