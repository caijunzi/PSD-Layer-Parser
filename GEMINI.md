# 项目持久化记忆（GEMINI.md）

> **本文件已精简为入口摘要**。
> 完整内容请以 `docs/` 下三份文档为准（单一真相来源，避免多处重复导致脏数据）：
>
> | 文档 | 内容 |
> | :--- | :--- |
> | `docs/UNIVERSAL_LAYER_ENGINE_DEVELOPMENT_PLAN.md` | **顶层开发计划（SSOT，v2.4）**：目标、架构、模块契约、流水线、质检、里程碑 |
> | `docs/MEMORY.md` | **ADR 决策记录、六条铁律、实测基线、psd-tools API 校准表、历史踩坑** |
> | `docs/DAILY_LOG.md` | 每日演化日志 |
> | `docs/BENCHMARK_REPORT.md` | **全量流水线效率实测与性能基准报告（秒级精确实测）** |
>
> **本文件仅保留最关键的铁律摘要与停止条件，任何与 docs/ 冲突的表述一律以 docs/ 为准。**

---

## 一、六条铁律（做任何改动前先对照）

| 编号 | 铁律 |
| :--- | :--- |
| R1 | 禁止人工伪色；输出像素走比色链路，**生成/补全内容必须标记为重建区** |
| R2 | 照度平场归一化**只服务检测分支**，不得写回输出像素 |
| R3 | 禁止裸 `vstack` 硬拼，接缝走流场对齐或几何平直裁切 |
| R4 | 核心轮廓几何保全，裁切线不得低于算子算出的安全下限 |
| R5 | 微孔检测仅在有效基材掩模内运行 |
| R6 | **层合成等价性**：可见层叠加必须还原原图（设计线硬约束） |

## 二、当前架构要点（v2.4）

- **双产品线**：`PLATE` 制版线（CMYK 工艺版层，给印刷厂）+ `DESIGN` 设计线（RGBA 元素层，给设计师微调/重组/排版）。共用内核，在 `compose` + `compile` 分叉。
- **业务图范围**：封闭 5 类（壁布 / 烫金 / 水墨 / 屏风 / 油画）。超出范围转人工。
- **硬件异构调度**：NVIDIA RTX 5070 Laptop (GPU.1, Blackwell sm_120) 借助 OpenVINO 激活为主加速器；Intel Arc 140T 16GB (GPU.0) 作为超大画幅防爆显存护盾；CPU/Telea 提供单切片级确定性熔断（Circuit Breaker）。
- **零硬编码自动化分层**：彻底消除硬编码坐标（24 个引擎文件全扫描通过），多尺度 Frangi 骨架流 + 自适应 Otsu 自动提取语义层。
- **C 扩展 SIMD 编码加速**：挂载 `imagecodecs` SIMD PackBits，解决 16K PSB 编码瓶颈（吞吐 200 MB/s，写盘从 19 分钟降至 27 秒）。
- **端到端极速并发流**：Step 4 遮挡补全（微边缘快速旁路 + 余弦 Hann 窗 + Batch 4 向量推理）与 Step 5 引导超分（局部 ROI 裁剪 + 6 物理核心 ThreadPool 并行），端到端整图（16000×7808，11 图层，150 PPI，2.34 GB）全流程耗时从 1468s 骤降至 **168s (2.80 分钟)**。
- **五维系统完整性与防欺骗审查**：严格执行环境真实探活、零硬编码静态扫描、物理交付物合规、代码防伪（零 mock/零 fake sleep）与高精度时钟基准审计。
- **坐标系**：`SRC → UNIT → CUT → CANVAS → PHYS` 五套，所有 Y 坐标必须带坐标系前缀。

## 三、已废止的旧表述（勿再引用）

| 旧表述 | 现状 |
| :--- | :--- |
| "100% 物理真实复原" | **废止**。改为 ΔE2000 均值 ≤ 2.0 / P95 ≤ 4.0（共同可印色域内） |
| "0.0 误差保留真实光学反光" | **废止**。RGB→CMYK 必经色域映射，不存在零误差 |
| "CMYK 4 通道 32 位深" | **错误**。实测为 **8 bit/channel** |
| "150.0 DPI" | 术语更正为 **150 PPI**（DPI 指输出设备） |
| 裁切安全线写死 Y=718 | 改为 `contour_protection` 运行时计算，718 仅作 preset 兜底 |
| 错位"近 100 像素" | 另有 80 像素说法，**以实测为准**，勿直接引用 |
| "pytoshop RLE 不可用/退回 RAW" | **废止**。已通过 `engine/codecs_accelerator.py` C-SIMD 彻底落地且实测 200 MB/s |
| "outputs/ 为空无成品" | **废止**。`outputs/Rosetsu_Master_16k.psb`（2.34 GB）已真实生成并验证 |

## 四、停止条件（遇到即停，先问再做）

1. 要修改 `engine/core/` 下任何文件 → 先确认是否属于内核变更。
2. 要在算子或流水线里写死坐标 / 阈值 → 必须外置到 `presets/*.json`。
3. 要引入新的重依赖（torch 等）→ 必须走 Provider + `requirements-ai.txt`，不得进主包。
4. 发现 `pipeline/`、`masks_16k/` 被视为废弃 → **错误**，它们是设计交付线的种子实现，正式维护中。
5. 核心运行与质检入口以 `run_universal_engine.py`、`pipeline/06_verify_psb.py`、`tests/audit_system_integrity.py` 为准，修改前先确认。
